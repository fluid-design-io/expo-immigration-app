{
  "name": "immigration-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "bun apps/server/src/index.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "mobile": "bun --filter @immigration/mobile start",
    "server": "bun --filter @immigration/server dev",
    "typecheck": "bun --filter '*' run typecheck"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~6.0.3"
  },
  "trustedDependencies": [
    "heroui-pro",
    "@heroui-pro/react",
    "heroui-native-pro"
  ]
}
