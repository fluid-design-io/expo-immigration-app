import { defineSchema } from 'convex/server'

// Better Auth manages its own tables in an isolated component namespace.
// The app schema is intentionally empty for the ground-up rebuild.
export default defineSchema({})
